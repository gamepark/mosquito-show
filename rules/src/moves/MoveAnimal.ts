import MoveType from "./MoveType"

/**
 * A "Move" is the combination of all the types of moves that exists in you game
 */
 type MoveAnimal = {type: typeof MoveType.MoveAnimal}
 
 export default MoveAnimal